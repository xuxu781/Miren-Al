# 项目目录结构说明

本文档详细介绍了 `lty-wzxt` 项目的整体目录结构及各主要文件的功能与用途。项目采用前后端分离架构，前端使用 Vue3 + Vite，后端使用 Go + Gin。

## 1. 根目录 (`e:\lty-wzxt`)

```text
lty-wzxt/
├── .trae/                  # Trae AI 代理生成的项目规范与任务记录文件
├── hd/                     # 后端及管理后台主目录 (Host Directory)
│   ├── lty_admin_ui/       # 前端 Vue3 管理后台界面项目
│   └── lty_backend/        # 后端 Go 语言服务端项目
├── 9ac2acd0-942c...png     # 项目相关截图或资源图片
└── README.md               # 项目主要说明文档（包含运行、部署与安装指南）
```

## 2. 前端管理后台 (`hd/lty_admin_ui`)

基于 Vue3 + Vite + TypeScript 构建的管理后台系统前端。

```text
lty_admin_ui/
├── .vscode/                # VS Code 编辑器配置 (如推荐扩展)
├── dist/                   # 生产环境编译打包后的静态资源目录 (npm run build 生成)
├── public/                 # 公共静态资源 (如 favicon.svg)，打包时直接复制到根目录
├── src/                    # 核心源代码目录
│   ├── assets/             # 项目内部静态资源 (图片、基础图标等)
│   ├── components/         # 可复用的 Vue 组件 (如 HelloWorld.vue)
│   ├── layout/             # 页面布局组件 (后台整体布局框架 index.vue)
│   ├── router/             # Vue Router 路由配置文件 (index.ts)
│   ├── views/              # 页面级视图组件
│   │   ├── Admin.vue       # 管理员设置/主页面
│   │   ├── Dashboard.vue   # 数据控制台/仪表盘主页
│   │   ├── Install.vue     # 可视化系统安装向导页面
│   │   └── Login.vue       # 系统登录页面
│   ├── App.vue             # Vue 根组件
│   ├── main.ts             # 前端项目入口文件，负责挂载应用及初始化插件
│   └── style.css           # 全局样式文件
├── index.html              # Vite 项目入口 HTML 文件
├── package.json            # Node.js 项目配置文件及依赖管理
├── package-lock.json       # 依赖版本锁定文件
├── tsconfig.*.json         # TypeScript 配置文件 (基础、Node、App配置等)
└── vite.config.ts          # Vite 构建工具配置文件
```

## 3. 后端服务端项目 (`hd/lty_backend`)

基于 Go 语言和 Gin 框架构建的后端接口服务。为了防止冲突，所有的包名和数据库表名均带有 `lty_` 前缀。

```text
lty_backend/
├── lty_config/             # 系统配置及数据库连接初始化模块
│   ├── config.go           # 配置文件读取与结构定义
│   └── db.go               # 数据库 (GORM) 实例初始化与连接逻辑
├── lty_controllers/        # 控制器层，处理具体的 HTTP 接口请求逻辑
│   ├── admin.go            # 管理员相关接口处理
│   ├── install.go          # 系统安装向导接口处理
│   └── user.go             # 用户相关接口处理
├── lty_middlewares/        # 中间件层
│   └── auth.go             # JWT 鉴权、登录拦截等安全中间件
├── lty_models/             # 数据模型层，定义数据库表结构 (GORM Models)
│   ├── admin.go            # 管理员实体模型
│   └── user.go             # 用户实体模型
├── lty_routers/            # 路由层
│   └── router.go           # 注册全局 API 路由及中间件应用
├── lty_utils/              # 工具函数包
│   ├── hash.go             # 密码加密解密 (如 bcrypt) 等哈希工具
│   └── jwt.go              # JWT Token 生成与解析验证工具
├── config.json             # 动态生成的系统配置文件 (数据库连接信息等)
├── go.mod / go.sum         # Go Module 依赖包管理与校验文件
├── lty_main.go             # Go 后端项目入口文件 (包含 main 函数)
├── lty_main.exe            # Windows 环境下编译生成的可执行文件
└── lty_backend_app         # Linux 环境下交叉编译生成的可执行文件
```

## 4. 架构与规范说明
- **前端规范**：页面级组件统一存放在 `src/views` 下，路由在 `router/index.ts` 中集中管理。
- **后端规范**：遵循 MVC 分层架构，请求流转路径为 `Router` -> `Controller` -> `Model`。各功能模块严格隔离，确保高内聚低耦合。
